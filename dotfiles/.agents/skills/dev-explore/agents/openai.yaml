interface:
  display_name: "Dev Explore"
  short_description: "Answer one focused repository or upstream question with compact "
  default_prompt: "Use $dev-explore for the requested workflow stage."
policy:
  allow_implicit_invocation: false
