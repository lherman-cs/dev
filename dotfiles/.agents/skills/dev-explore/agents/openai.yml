interface:
  display_name: "Dev Explore"
  short_description: "Investigate one repository question without editing"
  default_prompt: "Use $dev-explore for one focused repository question."

policy:
  allow_implicit_invocation: false
