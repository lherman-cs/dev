interface:
  display_name: "Plan Code Change"
  short_description: "Plan changes with Sol"
  default_prompt: "Use $dev:plan to investigate and plan this change."
policy:
  allow_implicit_invocation: false
