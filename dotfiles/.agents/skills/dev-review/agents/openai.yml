interface:
  display_name: "Dev Review"
  short_description: "Independent design, evidence, and integration review"
  default_prompt: "Use $dev-review with the exact project or plan path supplied."
policy:
  allow_implicit_invocation: false
