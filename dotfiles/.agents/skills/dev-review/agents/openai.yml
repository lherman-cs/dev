interface:
  display_name: "Dev Review"
  short_description: "Review one completed plan and write findings"
  default_prompt: "Use $dev-review with an exact completed plan path."

policy:
  allow_implicit_invocation: false
