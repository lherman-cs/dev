interface:
  display_name: "Dev Review"
  short_description: "Review an explicit candidate, repair, or final project"
  default_prompt: "Use $dev-review to review the explicit candidate and scope provided."
policy:
  allow_implicit_invocation: false
