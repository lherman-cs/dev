interface:
  display_name: "Dev Review"
  short_description: "Review one exact slice or explicitly assigned integration range "
  default_prompt: "Use $dev-review for the requested workflow stage."
policy:
  allow_implicit_invocation: false
