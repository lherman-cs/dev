interface:
  display_name: "Dev Spec"
  short_description: "Align and challenge an idea with the human, then write a readabl"
  default_prompt: "Use $dev-spec for the requested workflow stage."
policy:
  allow_implicit_invocation: false
