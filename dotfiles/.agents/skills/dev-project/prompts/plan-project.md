# Plan an approved project
Dispatch `planner` with `fork_turns="none"`.

Project: [PROJECT_DIR]. Worktree: [REPO]. Approved spec: [SPEC_FILE]. Current execution plan: [PLAN_FILE]. Current HEAD: [HEAD_SHA]. Read only the relevant recovery ledger/evidence, if present: [RECOVERY_PATHS].

Follow `dev-plan`. Define binding contracts, exact cross-task interfaces and proof cases; leave routine coding mechanics to the Builder. Tasks must justify one Builder and one Reviewer. Write one READY plan after self-review, or return the exact semantic/capability blocker. No implementation or separate plan reviewer.
