interface:
  display_name: "Dev Project"
  short_description: "Evidence-driven project execution and recovery"
  default_prompt: "Use $dev-project with the exact project or plan path supplied."
policy:
  allow_implicit_invocation: false
