interface:
  display_name: "Dev Build"
  short_description: "Implement and verify one numbered project plan"
  default_prompt: "Use $dev-build to implement exactly the specified numbered plan, verify it, create one coherent commit, and stop."

policy:
  allow_implicit_invocation: false
