interface:
  display_name: "Dev Build"
  short_description: "Implement and verify one approved plan"
  default_prompt: "Use $dev-build to implement one approved project plan with minimal rediscovery."

policy:
  allow_implicit_invocation: false
