interface:
  display_name: "Dev Build"
  short_description: "Implement and commit one coarse plan"
  default_prompt: "Use $dev-build to implement one approved coarse plan end-to-end, verify it, and commit it as one coherent deliverable."

policy:
  allow_implicit_invocation: false
