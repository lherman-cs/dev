interface:
  display_name: "Dev Build"
  short_description: "Implement and diagnose one approved plan"
  default_prompt: "Use $dev-build with the exact project or plan path supplied."
policy:
  allow_implicit_invocation: false
