interface:
  display_name: "Dev Build"
  short_description: "Implement and validate one executable plan or one bounded repair"
  default_prompt: "Use $dev-build for the requested workflow stage."
policy:
  allow_implicit_invocation: false
