interface:
  display_name: "Dev Plan"
  short_description: "Decompose an accepted spec into independently testable vertical "
  default_prompt: "Use $dev-plan for the requested workflow stage."
policy:
  allow_implicit_invocation: false
