interface:
  display_name: "Dev Plan"
  short_description: "Align and verify a project before implementation"
  default_prompt: "Use $dev-plan with the exact project or plan path supplied."
policy:
  allow_implicit_invocation: false
