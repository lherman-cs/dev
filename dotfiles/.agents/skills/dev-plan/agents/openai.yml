interface:
  display_name: "Dev Plan"
  short_description: "Design a bounded project and implementation plans"
  default_prompt: "Use $dev-plan to define this project, resolve consequential design decisions, and write the spec and numbered plans."

policy:
  allow_implicit_invocation: false
