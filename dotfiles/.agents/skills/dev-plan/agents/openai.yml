interface:
  display_name: "Dev Plan"
  short_description: "Define one bounded project and its implementation plans"
  default_prompt: "Use $dev-plan to resolve consequential ambiguity, define one bounded project, write its spec and ordered plans, then stop."

policy:
  allow_implicit_invocation: false
