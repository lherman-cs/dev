interface:
  display_name: "Dev Plan"
  short_description: "Decompose an approved spec into buildable pieces"
  default_prompt: "Use $dev-plan to turn an approved project spec into concrete implementation plans."

policy:
  allow_implicit_invocation: false
