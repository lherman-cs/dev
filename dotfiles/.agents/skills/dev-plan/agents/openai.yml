interface:
  display_name: "Dev Plan"
  short_description: "Plan one bounded project into coarse milestones"
  default_prompt: "Use $dev-plan to work with me as a senior engineer: bound one project, settle consequential design choices, and produce several coarse implementation plans."

policy:
  allow_implicit_invocation: false
