interface:
  display_name: "Dev Plan"
  short_description: "Plan one bounded change into executable milestones"
  default_prompt: "Use $dev-plan to define and plan one bounded software change."

policy:
  allow_implicit_invocation: false
