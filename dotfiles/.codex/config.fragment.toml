# Optional when using plain Codex, rather than `dev a ...`.
# Merge these policy values only; NEVER replace an existing config.toml.
[agents]
enabled = true
max_concurrent_threads_per_session = 4
interrupt_message = true
